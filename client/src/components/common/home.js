import React from 'react'
import { Container, Header } from 'semantic-ui-react'

const Home = (props) => {
    return (
        <Container>
            <Header as="h1" className="page-header">Home page</Header>
        </Container>
    );
};

export default Home;